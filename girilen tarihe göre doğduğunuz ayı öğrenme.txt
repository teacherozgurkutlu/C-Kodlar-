using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace ConsoleApplication4
{
    class Program
    {
        static void Main(string[] args)
        {
            string tarih;

            string[] parcalar;

            Console.Write("Tarihi Giriniz = ");

            tarih = Console.ReadLine();

            parcalar = tarih.Split('.');

            if (int.Parse(parcalar[1]) == 1) Console.WriteLine("DOÐDUÐUNUZ AY OCAK");

            if (int.Parse(parcalar[1]) == 2) Console.WriteLine("DOÐDUÐUNUZ AY ÞUBAT");

            if (int.Parse(parcalar[1]) == 3) Console.WriteLine("DOÐDUÐUNUZ AY MART");

            if (int.Parse(parcalar[1]) == 4) Console.WriteLine("DOÐDUÐUNUZ AY NÝSAN");

            if (int.Parse(parcalar[1]) == 5) Console.WriteLine("DOÐDUÐUNUZ AY MAYIS");

            if (int.Parse(parcalar[1]) == 6) Console.WriteLine("DOÐDUÐUNUZ AY HAZÝRAN");

            if (int.Parse(parcalar[1]) == 7) Console.WriteLine("DOÐDUÐUNUZ AY TEMMUZ");

            if (int.Parse(parcalar[1]) == 8) Console.WriteLine("DOÐDUÐUNUZ AY AÐUSTOS");

            if (int.Parse(parcalar[1]) == 9) Console.WriteLine("DOÐDUÐUNUZ AY EYLÜL");

            if (int.Parse(parcalar[1]) == 10) Console.WriteLine("DOÐDUÐUNUZ AY EKÝM");

            if (int.Parse(parcalar[1]) == 11) Console.WriteLine("DOÐDUÐUNUZ AY KASIM");

            if (int.Parse(parcalar[1]) == 12) Console.WriteLine("DOÐDUÐUNUZ AY ARALIK");

            Console.ReadKey(); 
 
        }
    }
}